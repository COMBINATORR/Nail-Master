🎯 **What:**
Added a test environment using Vitest, JSDOM, and React Testing Library to cover the testing gap for the main `App` component.

📊 **Coverage:**
- Verifies the main SVTL logo and "Nails & Aesthetic" subtitles render correctly without crashing.
- Tests that the application correctly initializes with "RU" language by default.
- Ensures the default 'dark' theme class is properly applied to `document.body` as a side effect on initialization.

✨ **Result:**
Increased frontend reliability by providing basic component mounting, interaction, and side-effect tests. Provides a solid foundation for adding future interaction tests and avoids regressions during refactoring.
